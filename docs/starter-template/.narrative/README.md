# .narrative/

This directory contains the declared narrative for this project.

- `canon/` — What we're saying. PR-protected.
- `skills/` — How we say it. Evaluation rules.
- `history/` — Score snapshots (auto-generated, git-ignored).

Edit the YAML files, then run `narrative check` to score your content.
